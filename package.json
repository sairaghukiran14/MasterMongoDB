{
  "name": "master-mongodb",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Master MongoDB with 100 hands-on challenges — runs real queries in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/validate-challenges.mjs"
  },
  "dependencies": {
    "mingo": "^6.4.13"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
